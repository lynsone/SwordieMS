# A Close Call - Start (3rd job adv for CoK)
def init():
    sm.chat("You found the old book Wiz was talking about. Report back to Neinhart.")
    sm.completeQuest(20882)
    sm.startQuest(20883)
    sm.startQuest(20884)