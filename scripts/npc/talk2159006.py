# Vita | Dangerous Hide-and-Seek : Suspicious Laboratory
def init():
    sm.sendSayOkay("This is taking too long!")

def action(response, answer):
    sm.warp(310000000, 0)
    sm.addLevel(9)
    sm.dispose()