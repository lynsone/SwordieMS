def init():
    sm.sendSay("SwordieMS uses python scripts!")

def action(response, answer):
    #sm.sendSay("Response was " + str(response))
    #sm.sendInitialQuiz(1, 2, 10, 4, 13, 20)
    #public void sendInitialQuiz(byte type, String title, String problem, String hint, int min, int max, int time) {
    sm.sendSay("You've kept me waiting, #e#b#h0##k#n. Shame on you! And after I went though all the trouble to obtain a brilliant #t1112726##n, too. You can make it up to me by accepting my challenge. \r\n#i1112726:# #t1112726#")
    sm.dispose()
